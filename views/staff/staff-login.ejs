<!doctype html>
<html lang="th">
<head>
    <title>เข้าสู่ระบบเจ้าหน้าที่</title>
    <%- include('partials/head') %>
    <script>
        // Service Worker Script (ไม่เปลี่ยนแปลง)
        if ('serviceWorker' in navigator)
            navigator.serviceWorker.register('/public/service-worker.js');
    </script>
    <style>
        /* ========================================================================= */
        /* =========================== STYLES: GLOBAL/VARS ========================= */
        /* ========================================================================= */
        :root {
            --brand: #1f8a4c; /* Green Brand Color */
            --brand-dark: #166a39;
            --bg: #f5f7fa; /* Light Background */
            --card: #ffffff;
            --ink: #1e293b;
            --line: #e4e7eb;
            --shadow-lg: 0 10px 30px rgba(0, 0, 0, 0.1);
            --border-radius: 12px;
        }
        
        body {
            font-family: 'Prompt', sans-serif;
            background-color: var(--bg);
            color: var(--ink);
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        /* Login Card */
        .login-card {
            background: var(--card);
            border: 1px solid #d1d5db;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow-lg);
            width: 100%;
            max-width: 440px; /* Consistent width with Admin Login */
            padding: 35px;
        }

        /* Header */
        .login-card h3 {
            font-weight: 700;
            text-align: center;
            margin-bottom: 25px;
            color: var(--brand-dark);
            font-size: 1.6rem;
        }

        /* Form Control */
        .form-control {
            border-radius: 8px;
            padding: 12px 15px; /* More padding */
            font-size: 1rem;
            border: 1px solid #d1d5db;
            box-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.05);
        }
        .form-control:focus {
            border-color: var(--brand);
            box-shadow: 0 0 0 0.25rem rgba(31, 138, 76, 0.25);
        }

        /* Button */
        .btn-primary {
            border-radius: 8px;
            padding: 12px 0;
            font-weight: 600;
            background-color: var(--brand); /* Use brand color */
            border-color: var(--brand);
            transition: background-color .2s;
        }
        .btn-primary:hover {
            background-color: var(--brand-dark);
            border-color: var(--brand-dark);
        }

        /* Alert */
        .alert-danger {
            margin-bottom: 20px;
            border-radius: 8px;
            font-size: 0.9rem;
            border: 1px solid #fca5a5;
            background-color: #fef2f2;
            color: #b91c1c;
        }

        /* Footer and Links */
        .footer-text {
            text-align: center;
            margin-top: 25px;
            padding-top: 15px;
            border-top: 1px solid #f3f4f6;
            font-size: 0.85rem;
            color: #9ca3af;
        }
        .admin-link a {
            color: #4b5563;
            text-decoration: none;
            font-weight: 500;
            transition: color .2s;
        }
        .admin-link a:hover {
            color: var(--brand);
        }
    </style>
</head>

<body>
    <div class="login-card">
        <h3><i class="bi bi-person-badge-fill me-2"></i>เข้าสู่ระบบเจ้าหน้าที่</h3>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <form method="post" action="/staff-login" class="vstack gap-3">
            <input class="form-control" type="email" name="email" placeholder="อีเมลเจ้าหน้าที่" required autofocus>
            <button class="btn btn-primary w-100" type="submit">
                <i class="bi bi-box-arrow-in-right me-2"></i> เข้าสู่ระบบ
            </button>
        </form>
        <div class="footer-text">
            © 2025 Sports & Fitness Center<br>
            มหาวิทยาลัยเทคโนโลยีราชมงคลกรุงเทพ
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html>