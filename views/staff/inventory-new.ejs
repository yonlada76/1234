<!DOCTYPE html>
<html lang="th">
<head>
    <title>เพิ่มอุปกรณ์ใหม่</title>
    <%- include('partials/head') %>
    <style>
        /* ========================================================================= */
        /* =========================== STYLES: GLOBAL/VARS ========================= */
        /* ========================================================================= */
        :root {
            --brand: #1f8a4c; /* Green Brand Color */
            --brand-dark: #166a39;
            --bg: #f5f7fa;
            --card: #ffffff;
            --ink: #1e293b;
            --line: #e4e7eb;
            --shadow-md: 0 8px 20px rgba(0, 0, 0, 0.08);
            --border-radius: 12px;
        }

        body {
            font-family: 'Prompt', sans-serif;
            background-color: var(--bg);
            color: var(--ink);
        }

        /* Card Style */
        .card {
            background: var(--card);
            border: 1px solid #d1d5db;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow-md);
        }

        /* Card Body Padding */
        .card-body {
            padding: 30px;
        }
        
        /* Header */
        h4 {
            font-weight: 700;
            color: var(--brand-dark);
            border-bottom: 2px solid #e0f2e8; /* Subtle underline */
            padding-bottom: 10px;
            margin-bottom: 25px !important;
        }

        /* Form Labels and Controls */
        .form-label {
            font-weight: 600;
            color: #374151;
            margin-bottom: 6px;
        }
        .form-control {
            border-radius: 8px;
            padding: 10px 15px;
            border: 1px solid #d1d5db;
            box-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.05);
            transition: border-color .15s ease-in-out, box-shadow .15s ease-in-out;
        }
        .form-control:focus {
            border-color: var(--brand);
            box-shadow: 0 0 0 0.25rem rgba(31, 138, 76, 0.25);
        }

        /* Alerts */
        .alert {
            border-radius: 8px;
            font-weight: 500;
        }

        /* Buttons */
        .btn-primary {
            background-color: var(--brand);
            border-color: var(--brand);
            font-weight: 600;
            padding: 10px 20px;
            border-radius: 8px;
            transition: background-color .2s;
        }
        .btn-primary:hover {
            background-color: var(--brand-dark);
            border-color: var(--brand-dark);
        }
        .btn-outline-secondary {
            border-color: #d0d5d9;
            color: #374151;
            font-weight: 500;
            padding: 10px 20px;
            border-radius: 8px;
        }
        .btn-outline-secondary:hover {
            background: #f2f4f6;
            color: #111;
        }
    </style>
</head>
<body>
    <div class="container" style="max-width:560px">
        <div class="card mt-5">
            <div class="card-body">
                <h4 class="mb-3"><i class="bi bi-box-seam-fill me-2"></i>เพิ่มอุปกรณ์ใหม่ในสต็อก</h4>

                <% if (error) { %>
                    <div class="alert alert-danger"><%= error %></div>
                <% } %>
                <% if (success) { %>
                    <div class="alert alert-success"><%= success %></div>
                <% } %>

                <form method="post" action="/staff/inventory/new" class="vstack gap-3">
                    <div class="mb-3">
                        <label class="form-label">ชื่ออุปกรณ์</label>
                        <input type="text"
                               name="name"
                               class="form-control"
                               placeholder="เช่น ลูกบาสเกตบอลเบอร์ 7"
                               required
                               value="<%= (form && form.name) ? form.name : '' %>">
                    </div>

                    <div class="mb-3">
                        <label class="form-label">สต็อกเริ่มต้น (จำนวน)</label>
                        <input type="number"
                               name="stock"
                               class="form-control"
                               min="0"
                               step="1"
                               placeholder="จำนวนเต็ม เช่น 10"
                               required
                               value="<%= (form && typeof form.stock !== 'undefined') ? form.stock : '' %>">
                    </div>

                    <div class="d-flex gap-3 pt-2">
                        <a class="btn btn-outline-secondary w-100" href="/staff/inventory">
                            <i class="bi bi-arrow-left me-2"></i>กลับหน้าจัดการสต็อก
                        </a>
                        <button type="submit" class="btn btn-primary w-100">
                            <i class="bi bi-plus-circle-fill me-2"></i>บันทึกอุปกรณ์ใหม่
                        </button>
                    </div>
                </form>

            </div>
        </div>
    </div>
    <%- include('partials/footer') %>
</body>
</html>